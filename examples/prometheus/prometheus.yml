global:
  scrape_interval: 10s
  evaluation_interval: 10s

  labels:
    monitor: django-monitor

rule_files:
- "django.rules"

scrape_configs:
- job_name: "django"
  target_groups:
  - targets: ["localhost:8000"]	
